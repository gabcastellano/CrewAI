# Multiagent CrewAI System — Using Groq (Free) + Mixtral 8x7B

Este projeto implementa um sistema **multiagente** simples inspirado em CrewAI,
usando 3 agentes:

- **Researcher Agent**
- **Curator Agent**
- **Writer Agent**

A função principal da crew é:
> Gerar um artigo técnico completo sobre um tema definido pelo usuário.

O sistema utiliza **Groq + Mixtral 8x7B**, totalmente gratuito.

---

## 🚀 Como rodar

### 1. Criar ambiente
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
